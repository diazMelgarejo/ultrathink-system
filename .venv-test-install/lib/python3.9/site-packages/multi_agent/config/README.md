# Configuration

## agent_registry.json
Defines all 7 agents: IDs, skill paths, logic paths, max instances, allowed tools.
Modify `max_instances` to scale agent capacity.

## routing_rules.json
Message routing rules with priority ordering.
Higher priority = evaluated first.
