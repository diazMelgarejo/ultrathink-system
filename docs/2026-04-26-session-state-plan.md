# Twin System Session State — April 26, 2026

> **Purpose:** Posterity record of what changed, what stayed the same, and what must happen next. Read this at the start of the next session. Both AlphaClaw and orama-system developers should read §3 (Immediate Next Steps) before touching anything.

---

## 1. System Snapshot (as of 2026-04-26)

### Three-Repo Architecture — Current State

```
AlphaClaw (Layer 1 — infrastructure)
  Branch: feature/MacOS-post-install (unchanged)
  State:  ✅ Stable — no changes this session
     │
     │  CLI + HTTP only (never require())
     ▼
Perpetua-Tools (Layer 2 — middleware)
  Branch: 2026-04-25-perpetua-recovery  ← NOT YET MERGED
  State:  9 commits ahead of origin/main — PR #21 open, awaiting merge
     │
     │  typed adapter contracts
     ▼
orama-system (Layer 3 — meta-intelligence)
  Branch: 2026-04-24-001-orama-salvage  ← NOT YET MERGED
  State:  8+ commits ahead of main — awaiting merge
```

### Confirmed Hardware & Endpoints

| Node | Local access | LAN access (from other node) | Status |
|------|-------------|------------------------------|--------|
| Mac M2 Pro (LM Studio) | `localhost:1234` | `192.168.254.105:1234` | ✅ Live |
| Win RTX 3080 (LM Studio) | `localhost:1234` | `192.168.254.103:1234` | ✅ Live |
| OpenClaw Gateway | `localhost:18789` | loopback only | ✅ Live |

> **Critical rule**: On Mac, always use `localhost:1234` for self-access. The `.105` IP is only for inbound connections from the Windows machine. This matches how the Win node uses its own `localhost` and exports `.103` for the Mac.

### Discovery Tier (current)

- **Tier 1** (both nodes live) — confirmed via `discover.py --force` on 2026-04-26
- Mac: 4 models loaded (gemma-4-26b, gemma-4-e4b, qwen3.5-27b-…-v2, qwen3.5-9b-mlx)
- Win: same 4 models (LM Link mirror)
- TTL: 5 minutes — run `discover.py --force` if stale

---

## 2. What Changed This Session

### 2a. IP Configuration (Critical Fix)

All configs were pointing to wrong Win IP (`192.168.254.101`, `.107`, `.109`). Win RTX 3080 was confirmed at `.103`.

| File | What changed |
|------|-------------|
| `~/.openclaw/openclaw.json` | `lmstudio-win.baseUrl`: `.101` → `.103` |
| `Perpetua-Tools/config/devices.yml` | win: `.101` → `.103`, mac LAN: `.107` → `.105` |
| `Perpetua-Tools/config/models.yml` | Auto-regenerated by `discover.py --force` |
| `Perpetua-Tools/.env.local` (gitignored) | `WINDOWS_IP`: `.109` → `.103` |
| `orama-system/start.sh` | IP references inherited via PT discovery — not directly patched |

### 2b. openclaw.json Model List Expansion

After IP fix + `discover.py --force`, model lists grew from 1 stub each to 4 real models per provider node.

### 2c. Perpetua-Tools Cleanup (9 commits on recovery branch)

| Commit | What it does |
|--------|-------------|
| `1a6b010` | Rescue: symlink portability note from unreachable Gemini commit `eebb5f9` into LESSONS.md |
| `2752d38` | Submodules updated to latest; `.gitmodules` dirty-flag suppressed |
| `38222f5` | Self-discovery skill v0.9.9.7 added |
| `8d8266e` | `agentic-stack` submodule added; `.gitignore` fixed |
| `4500be7` | Repo renamed Perplexity-Tools → Perpetua-Tools; LM Studio automation |
| `79a58bc` | git mv: skill dirs + instincts `Perplexity-Tools` → `Perpetua-Tools`; 18 ecc-tools.json paths |
| `622ca67` | Full sweep: `ultrathink→orama` + `Perplexity→Perpetua` in all docs/configs; Python bridge files renamed (`ultrathink_bridge.py` → `orama_bridge.py`, etc.) |
| `df7e94d` | **New skill**: `alphaclaw-session` v1.0.0 — env profiles, agent map, backup/restore |
| `fdbf749` | IP fix committed to branch |

### 2d. orama-system Changes

| Commit | What it does |
|--------|-------------|
| `f745604` | `start.sh`: 3 occurrences of `Perplexity-Tools` → `Perpetua-Tools` in PT discovery path |

### 2e. Forensic Recovery — 8 Unreachable Objects Explained

All 8 objects in `git fsck --unreachable` are accounted for. **Nothing was lost.**

| SHA | Type | Origin | Resolution |
|-----|------|---------|-----------|
| `eebb5f9` | commit | Gemini's "symlink portability" commit (bad auth identity) | Discarded intentionally; content rescued into LESSONS.md (`1a6b010`) |
| `8362ce0` | commit | Gemini's "3-tier IP detection" commit (bad auth identity) | Discarded intentionally; IP logic rebuilt correctly in `discover.py` |
| `ab197ef` | commit | Old combined submodule commit (force-pushed over) | Superseded by the split commits |
| `28dcbff` | stash object | Stash `stash@{1}` root (dropped stash) | Dropped — content was recovered before drop |
| `d0b62c0` | stash object | Another stash artifact | Same as above |
| `da4ffad` | unknown | Likely a stash index or intermediate rebase artifact | Confirmed unreachable from all refs; content not unique |
| `365dbad` | unknown | Likely another stash artifact | Same as above |
| `89aa413` | unknown | Likely intermediate rebase tree | Same as above |

---

## 3. What Stayed the Same

| Component | Unchanged |
|-----------|-----------|
| OpenClaw gateway | Port `18789`, loopback bind, same bearer token |
| 6 OpenClaw agents | `main`, `mac-researcher`, `win-researcher`, `orchestrator`, `coder`, `autoresearcher` — all unchanged |
| Three-repo architecture | AlphaClaw → Perpetua-Tools → orama-system boundaries respected |
| API keys | All in `~/.alphaclaw/.env` — OpenAI, Gemini (x2), GitHub — untouched |
| AlphaClaw source | Zero changes to AlphaClaw repo this session |
| MCP servers | `gemini-cli` + `ai-cli-mcp` registered in openclaw.json — unchanged |
| Telegram bot | `openclaw.json → channels.telegram` — unchanged |
| OTEL diagnostics | All enabled — unchanged |

---

## 4. Immediate Next Steps (Ordered by Priority)

### P0 — Merge Branches (blocking everything else)

```bash
# Step 1: Merge Perpetua-Tools recovery branch → main
gh pr merge 21 --merge --repo diazMelgarejo/Perpetua-Tools

# Step 2: Merge orama-system salvage → main
cd "/Users/lawrencecyremelgarejo/Documents/Terminal xCode/claude/OpenClaw/orama-system"
git checkout main && git pull origin main
git merge 2026-04-24-001-orama-salvage --no-ff -m "chore(merge): orama-system salvage → main"
git push origin main
```

### P1 — Rename Remaining Stale Skill Dir in orama-system

```bash
cd "/Users/lawrencecyremelgarejo/Documents/Terminal xCode/claude/OpenClaw/orama-system"
git mv .claude/skills/ultrathink-system .claude/skills/orama
git commit -m "feat(rename): ultrathink-system skill dir → orama"
```

### P2 — Fix Model ID Case Mismatch in openclaw.json

**Problem**: `discover.py` wrote lowercase model IDs to providers (e.g., `qwen3.5-27b-claude-4.6-opus-reasoning-distilled-v2`). But `openclaw.json` agent configs reference the original mixed-case ID (e.g., `lmstudio-win/Qwen3.5-27B-Claude-4.6-Opus-Reasoning-Distilled-v2`). Unknown if OpenClaw does case-insensitive match.

**Fix needed**: Verify by running OpenClaw and checking agent model resolution, or align cases. Update `~/.openclaw/openclaw.json` agent `model.primary` to match the lowercase IDs written by `discover.py`.

### P3 — alphaclaw-session v1.1 (DO's & DON'Ts + Self-Healing)

Update `.claude/skills/alphaclaw-session/SKILL.md` with:

```markdown
## DO's & DON'Ts

### DO
- Always run `discover.py --status` before starting any multi-agent session
- Use `--force` flag when Win IP has changed or nodes are suspected offline
- Keep `localhost:1234` for Mac self-referencing — NEVER use `.105` from Mac itself
- Use bearer token auth on every gateway request — loopback mode, no TLS
- Monitor usage-tracker plugin logs when on cloud (Tier 3) — costs are non-zero

### DON'T
- DON'T use `.101`, `.107`, `.109` — all wrong; Win is `.103`, Mac LAN is `.105`
- DON'T hardcode IPs in PT/orama scripts — always derive from `discover.py` or `~/.openclaw/openclaw.json`
- DON'T run discover.py without `--force` when debugging connectivity — TTL hides stale state
- DON'T require() AlphaClaw internals — CLI + HTTP only
- DON'T load models in parallel on Win — GPU loads ONE model at a time (RTX 3080 limit)
- DON'T skip `is_gpu_idle()` check before dispatching heavy tasks to coder agent

## Self-Healing Procedures

### Win node shows offline (Tier 2 fallback)
1. Ping Win: `ping -c 1 192.168.254.103` → if no response, Win is off or IP changed
2. If IP may have changed: check Win DHCP lease, update `.101`→new IP in:
   - `~/.openclaw/openclaw.json → models.providers.lmstudio-win.baseUrl`
   - `Perpetua-Tools/config/devices.yml → win.lan_ip`
   - `Perpetua-Tools/.env.local → WINDOWS_IP`
3. Run: `python3 ~/.openclaw/scripts/discover.py --force`
4. Verify: discover.py reports Tier 1

### Gateway unreachable (http_code 000)
1. Check if AlphaClaw process is running: `ps aux | grep alphaclaw`
2. Restart: `cd ~/.alphaclaw && node alphaclaw.js` (or via start.sh)
3. Re-verify: `curl -s -H "Authorization: Bearer d3aea7fea7ba51a1dff69b84662ae97d53dd3c2bcb182781" http://localhost:18789/health`
```

### P4 — Create Agent Definition Files

**Gemini-coder agent** (`.claude/agents/gemini-analyzer.md` in Perpetua-Tools):
- Role: Reader / large-context analyst
- API type: `google-generative-ai` (NOT `openai-completions` — gateway crashes)
- Model: `google/gemini-2.5-flash` or `google/gemini-3.1-pro-preview`
- MCP enabled: `gemini-cli`
- Use for: reading large codebases, cross-repo analysis, design review

**Codex/GPT-5.5 coder agent** (`.claude/agents/codex-coder.md`):
- Prompting pattern: outcome-first, short prompts, `low/medium` effort first
- Validate changes after each step
- Route through `ai-cli-mcp` in OpenClaw

### P5 — Self-Improve Skill (Idempotent)

Create `.claude/skills/self-improve/SKILL.md` in both PT and orama-system:
- Reads `docs/LESSONS.md` + instincts YAML
- Proposes minimal additive updates to skill files
- Version-guards against overwriting newer installed versions
- Idempotent: skip if current version ≥ bundled version

### P6 — Save Session to orama-system LESSONS.md

Append dated entry capturing:
- IP fix methodology (cross-config audit, `--force` discovery)
- Gemini commit rejection pattern (bad author identity → discard + rescue content)
- alphaclaw-session skill creation pattern (profile-based, not config-based)
- Discovery tier model (Tier 1-4 graceful degradation)

---

## 5. Open Questions / Known Unknowns

| Question | Impact | Resolution |
|----------|--------|-----------|
| Does OpenClaw model resolution do case-insensitive matching? | Agents may fail to load models | Test by dispatching task to `coder` agent; check logs |
| Should orama-system's `.claude/skills/orama-system/` be renamed to `orama`? | Consistency | Rename in P1 above |
| What is the Gbrain MCP server ID? (`mcp__gbrain__*`) | Needed for Codex bridge test | Confirmed available in deferred tools list — test when Codex is live |
| Is `agentic-stack` submodule still needed as Gbrain fallback? | Architecture decision | Keep for now; revisit once Gbrain MCP is validated |

---

## 6. Session Lessons (Key Takeaways)

1. **Always audit IPs across ALL config files simultaneously** — they drift. Use `grep -r "192.168.254" .` across all three repos after any hardware change.

2. **`discover.py --force` after any IP change** — the 5-min TTL caches stale state and hides connectivity problems.

3. **Gemini's commits cannot be merged** — bad author identities (`Lawrence@bettermind.ph`, `darth.serious@gmail.com` in commit author, `nimbosa` agent) must be discarded. Rescue content manually into LESSONS.md, then commit under correct identity.

4. **`git fsck --unreachable` is the truth oracle** — 8 objects, all explained. Nothing lost. Run this before panicking about missing commits.

5. **Python bridge file renames require test file renames too** — `ultrathink_bridge.py` had matching `test_ultrathink_bridge.py`, `test_ultrathink_mcp_client.py`, etc. Both layers must move together.

6. **Skill version guards prevent overwrite** — the `BUNDLED` pattern in alphaclaw-session ensures future automation doesn't regress an already-upgraded skill file.

7. **`localhost` is always right on the home node** — don't use LAN IPs for self-referencing. Mac uses `localhost:1234`, Win uses its own `localhost:1234`. LAN IPs are for cross-node calls only.

---

*Generated: 2026-04-26 | Branch: 2026-04-24-001-orama-salvage | Author: Claude (session recovery)*
*Next session: start with § 4 Immediate Next Steps, P0 first.*
