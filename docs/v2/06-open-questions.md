# 06 — Open Questions

Items deliberately deferred. Each has a target checkpoint for resolution.

---

## Active open questions

| # | Question | Context | Resolve by |
|---|----------|---------|------------|
| OQ1 | **Pydantic AI as framework** — at v2.1+, evaluate whether `pydantic-ai` (`Agent`, `Tool`, `RunContext`) should supplement or replace `MiniGraph` for the application layer (not kernel). | Pydantic AI is a framework built on Pydantic v2. It's a LangGraph competitor. MiniGraph is our custom engine. The question is whether v2.1's app layer uses one, both, or neither. | v2.1 checkpoint |
| OQ2 | **GGUF hardware spec extension** — a community RFC to add `system_requirements` to the GGUF metadata layer has been pending since Oct 2024 with no timeline. Do we wait for it, or does `agate` serve as the bridge (mapping GGUF model IDs to hardware policy)? | The GGUF format is the de facto standard for local model metadata. Adding hardware requirements to GGUF would let any GGUF loader natively enforce hardware affinity without a separate policy file. | agate v0.1 release |
| OQ3 | **`agate` naming** — Perplexity proposed "agate" as the name for the published hardware policy spec (memorable vs. `model-hardware-policy-spec`). The repo is confirmed as `oramasys/agate`. Should the Python package also be `agate` on PyPI? Check availability. | Name availability determines distribution strategy. | agate repo setup |
| OQ4 | **GitHub org `oramasys` creation** — user confirmed creating a real GitHub org. Steps: go to github.com/organizations/new → choose plan → name `oramasys`. Then transfer or create repos there. | Org creation is a manual step requiring the user to log in at github.com. Once done, run: `gh api orgs/oramasys` to verify, then `git remote set-url origin git@github.com:oramasys/{repo}.git` in each new repo. | Immediate (user action) |
| OQ5 | **Agent identity attestation (MAESTRO Layer 3)** — v2.5 MAESTRO Layer 3 requires agents to attest their identity per dispatch. Options: (a) cryptographic signing per agent process (secure, complex); (b) per-session UUID cached in state (simple, spoofable); (c) `metadata["agent_id"]` in PerpetuaState (already there, honor-system). | Cryptographic attestation may be overkill for a LAN-local system. Honor-system agent IDs may be sufficient until the system is exposed externally. | v2.5 safety planning |
| OQ6 | **Perpetua-Tools repository** — the 2026-04-28 revamp plan (Tasks 0 and 1) requires changes to `Perpetua-Tools` repo (CI blocker fix + Perplexity-Tools → Perpetua-Tools typo). PT is not available locally. Location: `github.com/diazMelgarejo/Perpetua-Tools`. | Need to `gh repo clone diazMelgarejo/Perpetua-Tools` to continue Tasks 0 and 1. | v1.0 RC (immediate) |
| OQ9 | **`quickstart.py` scope** — DX assessment flags TTHW ≤ 10 min as a target. `quickstart.py` should demonstrate: (a) 3-node graph, (b) hardware routing, (c) HITL pause + resume. Does it live in `perpetua-core/` or `oramasys/`? | Quickstart should live in `oramasys/` (it uses both repos). Ideally runnable with `uvicorn oramasys.orama.api.server:app --reload` + a separate `curl` or Python client. | v2.0 Phase 3 |
| OQ10 | **Capability-based routing** — Should `model_hardware_policy.yml` move from model-ID mapping to hardware capability rules (e.g. `VRAM >= 12GB`)? Current static mapping requires manual YAML updates for every new model. Rule-based routing would be more adaptive and resilient to model churn. | Would require a richer YAML schema and a query engine inside `HardwarePolicyResolver`. Static mapping is fast and debuggable; capability rules are expressive but add complexity. | v2.2 planning |
| OQ11 | **GossipBus background writer** — Does the SQLite-per-emit overhead necessitate an async background write queue? Current implementation connects/commits synchronously on every event. High-frequency nodes (>100 emits/sec) will bottleneck on I/O. | Options: (a) keep sync — simple, fine for Phase 1 loads; (b) thread-local WAL batching; (c) dedicated writer thread with `queue.Queue`. Phase 1 smoke tests pass but production dispatch rates have not been stress-tested. | Phase 2 before production run |
| OQ12 | **`MiniGraph.ainvoke` safety guard** — Should `max_steps` (default e.g. 100) be enforced to prevent infinite loops in malformed graphs? | Aligns with MAESTRO Layer 2 safety. A graph with a missing `END` edge currently runs forever. Trivial to add; deferred to keep Phase 1 engine minimal. | Phase 2 implementation |
| OQ13 | **`PerpetuaState` dataclass vs. `BaseModel`** — Phase 1 shipped `PerpetuaState` as a `pydantic.dataclasses.dataclass` (uses `replace()` for immutable update, `.to_dict()` / `.from_dict()` adapters). The original `01-kernel-spec.md` specced it as `BaseModel` (uses `.model_dump()`, `.model_validate()`). | `BaseModel` is required by the Phase 3 FastAPI HTTP layer for seamless serialization. Dataclass gives copy-on-write semantics naturally but requires adapter shims at the API boundary. Also note: `scratchpad` shipped as `str`, not `dict[str, Any]` as specced — same decision point. Must be resolved before Phase 3. See `15-phase1-as-built.md` Δ2. | Before Phase 3 (oramasys HTTP layer) |
| OQ14 | **`perpetua-core` org transfer** — Phase 1 kernel lives at `github.com/diazMelgarejo/perpetua-core`, not under the `oramasys` org. The `oramasys` GitHub org has not yet been created (OQ4 still open). | Transfer is one command once org exists: `gh api -X POST repos/diazMelgarejo/perpetua-core/transfer --field new_owner=oramasys`. Deferring until org is created avoids double-move churn. See `15-phase1-as-built.md` header note. | After OQ4 resolves |
| OQ15 | **`OramaToPTBridge` and `TaskPlan` migration timeline** — `OramaToPTBridge` (`bin/agents/dispatcher.py`) and `TaskPlan` schema (`bin/agents/orchestrator/task_schema.py`) currently live in orama-system v1 layer. They need to migrate to `oramasys` repo in Phase 3. | The v1 bridge becomes obsolete once `oramasys` fully replaces the PT supervisor. Phase 3 must decide: (a) migrate types before the oramasys HTTP layer ships; (b) run both in parallel until parity tests pass. See `15-phase1-as-built.md` §orama-system v1 additions. | Phase 3 planning |
| OQ16 | **`engine.py` integration vs. `graph/plugins/`** — Phase 1 shipped a ~130-line integrated `engine.py` (conditional edges, HITL, streaming, checkpointing all inline). The README.md D8 revision specifies "~70-line kernel + `graph/plugins/`". `00-context-and-decisions.md` D8 originally said "Tier 3 ~220 lines". Three descriptions, one actual state (~130 lines, no plugins dir). | Options: (a) **Canonicalize ~130-line integrated** — simpler, no plugin loading overhead, everything visible in one file; (b) **Refactor to ~70-line core + `plugins/`** — cleaner extension seam, easier third-party plugin loading, consistent with D4 microkernel. Decision shapes all Phase 2 graph work. See `15-phase1-as-built.md` Δ1. | Before Phase 2 graph work |
| OQ17 | **Typed `Message` wrapper vs. raw dict** — `perpetua_core/message.py` was specced in Phase 1 but not implemented. `messages` in `PerpetuaState` are plain `Dict[str, Any]` following OpenAI wire format directly. | A typed `Message(role, content, ...)` class adds IDE ergonomics and schema enforcement at the cost of coupling to a schema choice. Raw dicts are simpler and interoperable with any OpenAI-compatible client without conversion. Matters most when Phase 3 LLMClient wiring serializes messages. See `15-phase1-as-built.md` Δ6. | Before Phase 3 LLMClient wiring |

---

## Resolved (logged for posterity)

| # | Question | Resolution | Date |
|---|----------|------------|------|
| D1 | Clean-slate vs. evolve-in-place | Clean-slate rewrite | 2026-04-30 |
| D2 | Repo names | `perpetua-core` + `oramasys` | 2026-04-30 |
| D3 | v2 sequencing | v1.0 RC first | 2026-04-30 |
| D4 | Architecture model | Microkernel | 2026-04-30 |
| D5 | Plugin API | Internal v2.0, public v2.1 | 2026-04-30 |
| D6 | Spec layout | Master + per-module | 2026-04-30 |
| D7 | Schema lib | Pydantic v2 (Pydantic AI = framework, not schema lib) | 2026-04-30 |
| D8 | Kernel tier | Originally "Tier 3 (~220 lines)"; README revised to "~70-line + plugins" same day — **as-built: ~130 lines integrated, no plugins dir**. Full reconciliation deferred to OQ16. | 2026-04-30 / as-built 2026-05-14 |
| D9 | Build approach | GPT Phase 1-4, lift proven pieces | 2026-04-30 |
| D10 | License | MIT (matches LangChain + LangGraph) | 2026-04-30 |
| D11 | 3rd new repo | `oramasys/agate` (Hardware Policy Specification) | 2026-05-01 |
| D12 | GitHub org | Real GitHub org `oramasys` (user to create at github.com/organizations/new) | 2026-05-01 |
| D13 | TDD policy | Enshrined in `docs/v2/README.md` and `04-build-order.md`. tdd.md is the source of truth. | 2026-05-01 |
| OQ7 | Python version for new repos | **Partially resolved:** `perpetua-core` Phase 1 targets Python 3.9 (matches CI runtime; `requires-python = ">=3.9"` in `pyproject.toml`). 3.11 target deferred to `oramasys` repo. `perpetua-core` will bump to 3.11 in Phase 2 when `ExceptionGroup`/`tomllib` are needed. `str \| None` union syntax avoided in Phase 1 (uses `Optional[str]`) for 3.9 compat. | 2026-05-14 |
| OQ8 | `optimize_for` vs. `opt_hint` field name | **Resolved:** `optimize_for` is canonical. Lives on `TaskPlan` (planning objects), not on `PerpetuaState`. `PerpetuaState.metadata` is the extension point if routing hints need to flow through the graph. Matches GPT scaffold and policy routing key structure. | 2026-05-14 |
